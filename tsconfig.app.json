{
  "compilerOptions": {
    "tsBuildInfoFile": "./node_modules/.tmp/tsconfig.app.tsbuildinfo",
    "target": "es2023",
    "lib": ["ES2023", "DOM"],
    "module": "esnext",
    "types": ["vite/client"],
    "allowArbitraryExtensions": true,
    "skipLibCheck": true,

    /* Mirrors the "resolve.alias" entry in vite.config.ts — keep the two in step.
     * No "baseUrl": TS 6 deprecates it, and under moduleResolution:"bundler" these
     * paths already resolve relative to this file. */
    "paths": { "@/*": ["./src/*"] },

    /* Bundler mode */
    "moduleResolution": "bundler",
    "allowImportingTsExtensions": true,
    "verbatimModuleSyntax": true,
    "moduleDetection": "force",
    "noEmit": true,
    "jsx": "react-jsx",

    /* Linting */
    /* Strict costs us nothing today — the app was written against strict-shaped
     * types from the start — and it is the only thing that keeps null/undefined
     * from a flaky addon response reaching the player as a runtime crash on a TV,
     * where there is no console to read and no way to ship a hotfix quickly. */
    "strict": true,
    "noUnusedLocals": true,
    "noUnusedParameters": true,
    "erasableSyntaxOnly": true,
    "noFallthroughCasesInSwitch": true
  },
  "include": ["src"]
}
